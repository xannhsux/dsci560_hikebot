version: "3.9"

services:
  api:
    image: python:3.11-slim
    working_dir: /app
    # ⚠️ 注意：为了确保 openai 库被安装，这里 command 会自动安装 requirements.txt
    command: >
      bash -c "pip install --no-cache-dir -r requirements.txt
      && uvicorn app:app --host 0.0.0.0 --port 8000 --reload"
    volumes:
      - ./backend:/app
    env_file:
      - ./backend/.env.example
    environment:
      - PYTHONUNBUFFERED=1
      # 👇 新增：设置假的 Key，防止 OpenAI 库报错
      - OPENAI_API_KEY=ollama
    ports:
      - "8000:8000"
    depends_on:
      - postgres
      - redis
    # 👇👇👇 关键修改：允许容器访问宿主机网络 (Ollama) 👇👇👇
    extra_hosts:
      - "host.docker.internal:host-gateway"

  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: hikebot
      POSTGRES_USER: hikebot
      POSTGRES_PASSWORD: hikebot
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./backend/migrations:/app/migrations:ro

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"

  ui:
    # 👇 改动 1：使用本地构建，而不是直接拉取 python 镜像
    build: ./frontend
    container_name: dsci560_hikebot-ui-1
    working_dir: /app
    # 👇 改动 2：命令只负责运行，不再负责安装。加上 fileWatcherType=poll 确保代码热更新生效
    command: streamlit run app.py --server.port 8501 --server.address 0.0.0.0 --server.fileWatcherType=poll
    volumes:
      - ./frontend:/app
    env_file:
      - ./frontend/.env.example
    environment:
      - PYTHONUNBUFFERED=1
    ports:
      - "8501:8501"
    depends_on:
      - api
volumes:
  postgres-data: